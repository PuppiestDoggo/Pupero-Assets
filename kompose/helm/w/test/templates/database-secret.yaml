apiVersion: v1
kind: Secret
metadata:
  name: {{ include "pupero.fullname" . }}-database-secret
  labels:
    {{- include "pupero.labels" . | nindent 4 }}
type: Opaque
stringData:
  # Use .Values.database.rootPassword.value (override with --set or external secret)
  mysql-root-password: {{ .Values.database.rootPassword.value | quote }}
