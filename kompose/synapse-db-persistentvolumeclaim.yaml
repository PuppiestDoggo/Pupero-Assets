apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: synapse-db
  name: synapse-db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
