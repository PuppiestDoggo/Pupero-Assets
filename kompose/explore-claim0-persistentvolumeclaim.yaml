apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: explore-claim0
  name: explore-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
